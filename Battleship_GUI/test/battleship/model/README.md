# Model Tests

This package contains unit tests for the `BattleshipGame` model and `Cards` class. 
The tests can be started individually by right-clicking the file and run it.
All tests can be started by right-clicking the package `test` and selecting `Run All Tests in 'test.battleship.model'`.